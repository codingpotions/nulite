---
title: TEST
layout: layouts/default.liquid
---

<div class="main-sections">
  <section>
    <h2>Latest</h2>
    <div class="article">
      <a href="/buy-wisely">
        <h3 class="article-title">Buy wisely</h3>
        <div class="article-time">
          <time datetime="2023-09-30T00:00:00+00:00">September 30, 2023</time>
          <span class="faint">•</span>
          <span class="reading-time" title="Estimated read time">
            5 minute read
          </span>
        </div>
        <div class="article-description">
          Whenever I buy things I try to prioritize cost per use. Sometimes I
          consider other priorities such as cost per smile, cost per thrill,
          cost per externality, and cost per lesson. Cost per use Cost per use
          is a heuristic that helps me make decisions about most non-perishable
          purchases such... Keep&nbsp;reading&nbsp;→
        </div>
      </a>
    </div>
  </section>

  <section>
    <h2>Topics</h2>
      {% assign tagList = collections | postTags %}
      {%- for tag in tagList -%}
      <a href="/tag/{{tag.name}}">{{tag.name}}({{tag.count}})</a>{% if forloop.last != true %}<span>, </span>{% endif %}
      {%- endfor -%}
  </section>
</div>

<style>
  .main-sections {
    display: flex;
    flex-direction: column;
    gap: var(--space-7);
  }
  .main-sections h2 {
    color: var(--color-text-shade);
    font-weight: normal;
    margin-bottom: var(--space-2);
  }
  .article {
    padding: var(--space-2) 0;
  }
  .article a {
    color: var(--color-text);
    text-decoration: unset;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  .article-title {
    transition: color 0.2s ease-in-out;
  }
  .article-description {
    font-size: var(--font-size-xs);
  }
  .article:hover .article-title {
    color: var(--color-primary);
  }
  .article-time {
    color: var(--color-text-shade);
    font-size: var(--font-size-xs);
  }
</style>
